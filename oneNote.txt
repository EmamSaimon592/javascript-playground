The Conditional (Ternary) Operator:
let text = (age < 18) ? "Minor" : "Adult";

1.Set হলো এমন এক ধরনের collection, যেখানে একই মান (duplicate) রাখা যায় না।
মানে — প্রতিটি ভ্যালু একবারই থাকবে।

const numbers = new Set([1, 2, 3, 3, 4]);
console.log(numbers); // Output: Set(4) {1, 2, 3, 4}

নতুন আইটেম যোগ করা:
numbers.add(5);
console.log(numbers); // Set(5) {1, 2, 3, 4, 5}

কোনো আইটেম আছে কিনা চেক করা:
console.log(numbers.has(3)); // true
console.log(numbers.has(6)); // false

আইটেম ডিলিট করা:
numbers.delete(2);
console.log(numbers); // Set(4) {1, 3, 4, 5}

লুপ করে দেখা:
for (let num of numbers) {
  console.log(num);
}


2. Map method 
Map হলো key-value pair এর একটি ডেটা স্ট্রাকচার,
যেখানে key যেকোনো টাইপের হতে পারে (string, number, object)।

const person = new Map();

person.set("name", "Imam");
person.set("age", 21);

console.log(person.get("name")); // Imam
console.log(person.size);        // 2


3. Callback Function
Callback function মানে হলো —
একটা ফাংশনের ভেতরে আরেকটা ফাংশন argument হিসেবে দেওয়া,
যেটা পরে call করা হবে।

function greet(name, callback) {
  console.log("Hello " + name);
  callback();  // এখানে callback function call হচ্ছে
}

function sayBye() {
  console.log("Goodbye!");
}

greet("Imam", sayBye);


৫. setTimeout()
setTimeout() দিয়ে নির্দিষ্ট সময় পর (delay দিয়ে) কোনো ফাংশন চালানো যায়।

console.log("1. শুরু হচ্ছে...");

setTimeout(() => {
  console.log("2. 3 সেকেন্ড পর দেখা হলো 😴");
}, 3000);

console.log("3. কোড শেষ...");
